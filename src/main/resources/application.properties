spring.application.name=api

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/project_management

# JWT
# Secret must be at least 32 characters (256 bits) for HMAC-SHA256
jwt.secret=change-this-to-a-secure-random-256-bit-secret-value
jwt.expiration=86400000
